📦 ПАПКА ИМПОРТА ДАННЫХ В PRAGMATICA

Назначение:
Хранит все инструменты для импорта данных в систему Pragmatica
(функции, структура изделия, связи и т.д.).

───────────────────────────────
СТРУКТУРА

• Функции
  – Импорт функциональной структуры (иерархия функций ФИ и систем).
  – Запуск через ЗАПУСК.bat.
  – В папке есть всё необходимое: GUI, шаблон, результаты и инструкции.

• Структура изделия
  – Импорт структуры изделия из Excel.
  – Использует аналогичный механизм (Excel → XML).
  – Подробности см. в README внутри папки.

• logo
  – Логотипы, используемые в интерфейсе.

───────────────────────────────
ОБЩИЕ ПРАВИЛА

• Все bat-файлы запускаются двойным кликом.
• Excel-файлы не должны быть открыты во время запуска.
• Все результаты (XML и Excel) сохраняются в своих подпапках.
• Старые версии автоматически перемещаются в подпапку «Архив».
• Для каждого раздела (Функции, Структура изделия) есть свой README и шаблон.

───────────────────────────────
ТРЕБОВАНИЯ К ОКРУЖЕНИЮ

Нужен установленный Python 3 и следующие внешние библиотеки:

    pip install pandas openpyxl customtkinter python-docx pymupdf pillow

Назначение:
– pandas         – работа с таблицами и Excel;
– openpyxl       – движок для .xlsx (используется pandas);
– customtkinter  – графический интерфейс;
– python-docx    – чтение .docx;
– pymupdf (fitz) – чтение и разбор PDF;
– pillow (PIL)   – работа с изображениями (логотип в GUI).

Проверка Python:
    python --version

Если команда не найдена:
– установи Python с официального сайта;
– при установке обязательно включи опцию «Add Python to PATH».

После этого ЗАПУСК.bat и графический интерфейс будут работать.

───────────────────────────────
УСТАНОВКА И НАСТРОЙКА ОКРУЖЕНИЯ

1) Скачай и установи Python 3 (https://www.python.org/downloads/)
   При установке поставь галочку:
   [x] Add Python to PATH

2) Проверь установку:
   python --version

3) Установи библиотеки:
   pip install pandas openpyxl customtkinter python-docx pymupdf pillow

4) После этого:
   – запусти ЗАПУСК.bat;
   – окно «Импорт функций в Pragmatica» откроется автоматически.